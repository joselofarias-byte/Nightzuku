name: Verify GitHub build fix

on:
  push:
    branches:
      - fix/github-build-20260815

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Setup Android SDK
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses > /dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
      - name: Restore tracked wrapper jar and build manager
        run: |
          cp api/gradle/wrapper/gradle-wrapper.jar gradle/wrapper/gradle-wrapper.jar
          chmod +x gradlew
          ./gradlew :manager:assemble --stacktrace
      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: nightzuku-apks
          path: out/apk/*.apk
          if-no-files-found: error
